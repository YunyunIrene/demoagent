0.git clone the code and enter the folder\
1.create environment： python -m venv venv && source venv/bin/activate\
2.install copilot sdk:pip install github-copilot-sdk\
3.change "Your token" in line 92 into the string of your copilot token\
4.Run:python main.py
